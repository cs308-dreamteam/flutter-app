import 'package:flutter/material.dart';

class UserProfileAnalysis extends StatelessWidget {
  const UserProfileAnalysis({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
