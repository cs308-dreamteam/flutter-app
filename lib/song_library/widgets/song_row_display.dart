import 'package:flutter/material.dart';

import '../../models/song.dart';

class SongRowDisplay extends StatelessWidget {
  final Song song;
  const SongRowDisplay({Key? key, required this.song}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
