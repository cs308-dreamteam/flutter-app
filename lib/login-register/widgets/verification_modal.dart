import 'package:flutter/material.dart';

class VerificationModal extends StatelessWidget {
  const VerificationModal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: Add verification modal
    return Container();
  }
}
